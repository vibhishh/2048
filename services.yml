apiVersion: v1
kind: Service
metadata:
  name: game-service
  namespace: gamespace

spec:
  selector:
    # Selector for Pods
    app: game-app
  ports:
    # Port Map
  - port: 6000
    targetPort: 6000
    protocol: TCP
  type: NodePort
